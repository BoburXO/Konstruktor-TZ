import Loading from "react-fullscreen-loading";

export default function Loader() {
  return <Loading loading background="#fff" loaderColor="#3498db" />;
}

const a = {
  name: "mirshod",
  age: 16,
};
